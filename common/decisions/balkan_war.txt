GOL_balkan_war_decisions_category = {
	HUN_request_megastructure = {
		icon = x
		fire_only_once = yes
		allowed = {
			tag = HUN
		}
		visible = {
			has_global_flag = GOL_flag_balkan_war_started
			NOT = { has_global_flag = GOL_flag_GER_approved_megastructure }
			NOT = { has_country_flag = GOL_flag_HUN_requested_megastructure }
		}
		available = {
			has_political_power > 75
		}
		cost = 75
		days_remove = 7
		complete_effect = {
			hidden_effect = {
				set_country_flag = GOL_flag_HUN_requested_megastructure
				GER = {
					country_event = { #匈牙利请求改进型巨构机
						id = GER.2
					}
				}
			}
		}
		ai_will_do = {
			base = 100
		}
	}
	HUN_The_war_has_reached_a_stalemate = {
		icon = x
		fire_only_once = yes
		activation = {
         has_idea = HUN_war_1
		 	has_global_flag = GOL_flag_balkan_war_started
			NOT = { has_global_flag = GOL_flag_GER_approved_megastructure }
			NOT = { has_country_flag = GOL_flag_HUN_requested_megastructure }
        }
        available = {
         always = no
        }
		visible = {
		
		}

	days_mission_timeout = 400
    selectable_mission = no
    complete_effect = {

    }
    timeout_effect = {
         swap_ideas = {
        remove_idea = HUN_war_1
        add_idea = HUN_war_2
       }
	   MAC = {
		remove_ideas = MAC_war_1
	   }
	   activate_mission = HUN_Frontline_collapse
		}
		ai_will_do = {
			base = 100
		}
	}
	HUN_Frontline_collapse = {
		icon = x
		fire_only_once = yes
		activation = {
		always = no
         has_idea = HUN_war_2
        }
        available = {
			NOT = {
				 has_idea = HUN_war_2
			}       
        }
		visible = {
		
		}

	days_mission_timeout = 360
    selectable_mission = no
    complete_effect = {

    }
    timeout_effect = {
        swap_ideas = {
        remove_idea = HUN_war_2
        add_idea = HUN_war_3
       }
		}
	ai_will_do = {
			base = 100
		}
	}
}